# Piper-stemmen

Deze modellen worden door `say-piper-tts` gebruikt. Home Manager koppelt elk
model en het bijbehorende `.onnx.json`-bestand aan
`~/.local/share/piper-voices`.

`nl_NL-pim-medium` is de standaardstem voor `say piper`. De grote `.onnx`
bestanden worden met Git LFS beheerd.
