interface:
  display_name: "Resolving Merge Conflicts"
  short_description: "Resolve merge and rebase conflicts"
  default_prompt: "Use $resolving-merge-conflicts to finish this merge or rebase by preserving both sides' intent."
