interface:
  display_name: "Triage"
  short_description: "Move issues through triage roles"
  default_prompt: "Use $triage to classify and prepare the issues that need attention."
policy:
  allow_implicit_invocation: false
