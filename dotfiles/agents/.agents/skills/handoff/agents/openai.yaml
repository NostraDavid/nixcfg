interface:
  display_name: "Handoff"
  short_description: "Compact a conversation into a handoff"
  default_prompt: "Use $handoff to prepare the current conversation for a fresh agent session."
policy:
  allow_implicit_invocation: false
