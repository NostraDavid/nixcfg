interface:
  display_name: "To Spec"
  short_description: "Turn a conversation into a spec"
  default_prompt: "Use $to-spec to turn this conversation into a tracker-ready specification."
policy:
  allow_implicit_invocation: false
